/**
 * Production environment configuration.
 *
 * Set `apiBaseUrl` to the absolute origin where the TODO backend API is hosted
 * (for example `https://api.example.com`). It must NOT end with a trailing
 * slash. Leave it empty only if the API is served from the same origin as the
 * built frontend.
 */
export const environment = {
  production: true,
  apiBaseUrl: '',
};
